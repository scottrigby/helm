name: "postrenderer"
version: "1.2.3"
type: postrenderer/v1
apiVersion: v1
runtime: subprocess
config:
  postrendererArgs: []
runtimeConfig:
  platformCommand:
    - command: "${HELM_PLUGIN_DIR}/sed-test.sh"
