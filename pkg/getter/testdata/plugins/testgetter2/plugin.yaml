name: "testgetter2"
version: "0.1.0"
type: getter/v1
apiVersion: v1
runtime: subprocess
config:
  protocols:
    - "test2"
runtimeConfig:
  # TODO remove command?
  command: "$HELM_PLUGIN_DIR/get.sh"
  protocolCommands:
    - command: "echo"
      protocols:
        - "test2"
