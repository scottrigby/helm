name: "testgetter"
version: "0.1.0"
type: download
apiVersion: v1
runtime: subprocess
config:
  downloaders:
    - command: "echo"
      protocols:
        - "test"
runtimeConfig:
  command: "$HELM_PLUGIN_DIR/get.sh"
