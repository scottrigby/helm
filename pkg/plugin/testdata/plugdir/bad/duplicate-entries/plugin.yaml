name: "duplicate-entries"
version: "0.1.0"
type: cli
apiVersion: v1
runtime: subprocess
config:
  usage: "usage"
  description: |-
    description
  ignoreFlags: true
runtimeConfig:
  command: "echo hello"
  hooks:
    install: "echo installing..."
  hooks:
    install: "echo installing something different"