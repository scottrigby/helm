name: "downloader"
version: "1.2.3"
type: getter/v1
apiVersion: v1
runtime: subprocess
config:
  - protocols:
      - "myprotocol"
      - "myprotocols"
runtimeConfig:
  # TODO remove command?
  command: "echo Hello"
  protocolCommands:
    - protocols:
        - "myprotocol"
        - "myprotocols"
      command: "echo Download"
