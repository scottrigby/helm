name: "downloader"
version: "1.2.3"
type: download
apiVersion: v1
runtime: subprocess
config:
  downloaders:
    - protocols:
      - "myprotocol"
      - "myprotocols"
      command: "echo Download"
runtimeConfig:
  command: "echo Hello"
